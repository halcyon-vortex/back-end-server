machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t halcyon-vortex/back-end-server .

test:
  override:
    - docker run -it halcyon-vortex/back-end-server exec npm test